f = r'C:\caas-dashboard\frontend\src\MigrationPage.jsx'
raw = open(f, 'rb').read()

# CHANGE 1: Add liveMtvStatus state after liveStatus
old1 = b'const [liveStatus, setLiveStatus] = useState("");'
new1 = b'const [liveStatus, setLiveStatus] = useState("");\r\n  const [liveMtvStatus, setLiveMtvStatus] = useState(null);'
assert old1 in raw, "CHANGE 1 not found"
raw = raw.replace(old1, new1)
print("1. Added liveMtvStatus state")

# CHANGE 2: Store mtv_status during polling (in the interval)
old2 = b'setLiveStatus(r.status || "");\r\n        if (["completed", "failed"'
new2 = b'setLiveStatus(r.status || "");\r\n        if (r.mtv_status) setLiveMtvStatus(r.mtv_status);\r\n        if (["completed", "failed"'
assert old2 in raw, "CHANGE 2 not found"
raw = raw.replace(old2, new2)
print("2. Store mtv_status in polling interval")

# CHANGE 3: Store mtv_status in initial fetch too
old3 = b'setLiveStatus(r.status || "");\r\n    }).catch(() => {});'
new3 = b'setLiveStatus(r.status || "");\r\n      if (r.mtv_status) setLiveMtvStatus(r.mtv_status);\r\n    }).catch(() => {});'
assert old3 in raw, "CHANGE 3 not found"
raw = raw.replace(old3, new3)
print("3. Store mtv_status in initial fetch")

# CHANGE 4: Add MTV pipeline stages component after progress bar
# Find the closing </div> of the progress bar section, right before actions
old4 = b'</div>\r\n                            )}\r\n                            {actions.length > 0 && ('
assert old4 in raw, "CHANGE 4 marker not found"

mtv_pipeline = b'''</div>\r\n                            )}\r\n                            {/* MTV Pipeline Stages - VMware to OpenShift only */}\r\n                            {plan.target_platform === "openshift" && isLive && liveMtvStatus && liveMtvStatus.vms && liveMtvStatus.vms.length > 0 && (\r\n                              <div style={{ marginBottom: 14, padding: 14, borderRadius: 8, background: `${p.surface}`, border: `1px solid ${p.border}` }}>\r\n                                <div style={{ fontSize: 13, fontWeight: 800, color: p.text, marginBottom: 10, textTransform: "uppercase", letterSpacing: ".5px" }}>\xf0\x9f\x94\x84 MTV Migration Stages</div>\r\n                                {liveMtvStatus.vms.map((vm, vi) => {\r\n                                  const MTV_STAGES = ["Initialize","PreflightInspection","DiskTransfer","Cutover","ImageConversion","VirtualMachineCreation"];\r\n                                  const stageMap = {};\r\n                                  (vm.pipeline || []).forEach(s => { stageMap[s.name] = s; });\r\n                                  return (\r\n                                    <div key={vi} style={{ marginBottom: vi < liveMtvStatus.vms.length - 1 ? 12 : 0 }}>\r\n                                      <div style={{ fontSize: 12.5, fontWeight: 700, color: p.text, marginBottom: 8 }}>\xf0\x9f\x96\xa5\xef\xb8\x8f {vm.name || "VM"} <span style={{ fontSize: 11, fontWeight: 500, color: p.textMute, marginLeft: 6 }}>Phase: {vm.phase || "Pending"}</span></div>\r\n                                      <div style={{ display: "flex", alignItems: "center", gap: 0 }}>\r\n                                        {MTV_STAGES.map((stg, si) => {\r\n                                          const info = stageMap[stg] || (stg === "DiskTransfer" ? stageMap["DiskTransferV2v"] || stageMap["DiskAllocation"] : null) || {};\r\n                                          const phase = info.phase || "Pending";\r\n                                          const isComplete = phase === "Completed";\r\n                                          const isRunning = phase === "Running" || phase === "InProgress";\r\n                                          const isFailed = phase === "Failed" || phase === "Error";\r\n                                          const isPending = !isComplete && !isRunning && !isFailed;\r\n                                          const color = isComplete ? "#22c55e" : isRunning ? "#3b82f6" : isFailed ? "#ef4444" : `${p.textMute}55`;\r\n                                          const icon = isComplete ? "\\u2714" : isRunning ? "\\u23F3" : isFailed ? "\\u2718" : "\\u25CB";\r\n                                          const pct = info.total > 0 ? Math.round(info.completed / info.total * 100) : null;\r\n                                          const label = stg.replace(/([A-Z])/g, " $1").trim();\r\n                                          return <Fragment key={stg}>\r\n                                            <div style={{ display: "flex", flexDirection: "column", alignItems: "center", flex: 1, minWidth: 0 }}>\r\n                                              <div style={{ width: 30, height: 30, borderRadius: "50%", background: isPending ? `${p.textMute}15` : `${color}18`, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 14, border: `2px solid ${color}`, boxShadow: isRunning ? `0 0 10px ${color}44` : "none", transition: "all .3s", animation: isRunning ? "pulse 2s infinite" : "none" }}>\r\n                                                <span style={{ color, fontWeight: 700 }}>{icon}</span>\r\n                                              </div>\r\n                                              <div style={{ fontSize: 10, marginTop: 3, color: isPending ? p.textMute : color, fontWeight: isRunning ? 800 : 600, textAlign: "center", lineHeight: 1.2, maxWidth: 80 }}>{label}</div>\r\n                                              {pct !== null && <div style={{ fontSize: 9, color, fontWeight: 700, marginTop: 1 }}>{pct}%</div>}\r\n                                              {info.description && <div style={{ fontSize: 9, color: p.textMute, marginTop: 1, textAlign: "center", maxWidth: 90, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{info.description}</div>}\r\n                                            </div>\r\n                                            {si < MTV_STAGES.length - 1 && <div style={{ flex: 0.6, height: 2, borderRadius: 1, background: isComplete ? "#22c55e" : `${p.textMute}22`, transition: "background .5s", marginBottom: 20 }} />}\r\n                                          </Fragment>;\r\n                                        })}\r\n                                      </div>\r\n                                    </div>\r\n                                  );\r\n                                })}\r\n                              </div>\r\n                            )}\r\n                            {/* Also show MTV stages for completed plans */}\r\n                            {plan.target_platform === "openshift" && plan.status === "completed" && plan.mtv_status && plan.mtv_status.vms && plan.mtv_status.vms.length > 0 && (\r\n                              <div style={{ marginBottom: 14, padding: 14, borderRadius: 8, background: `${p.surface}`, border: `1px solid ${p.border}` }}>\r\n                                <div style={{ fontSize: 13, fontWeight: 800, color: "#22c55e", marginBottom: 10, textTransform: "uppercase", letterSpacing: ".5px" }}>\\u2714 MTV Migration Stages (Completed)</div>\r\n                                {plan.mtv_status.vms.map((vm, vi) => {\r\n                                  const MTV_STAGES = ["Initialize","PreflightInspection","DiskTransfer","Cutover","ImageConversion","VirtualMachineCreation"];\r\n                                  return (\r\n                                    <div key={vi} style={{ marginBottom: 4 }}>\r\n                                      <div style={{ fontSize: 12.5, fontWeight: 700, color: p.text, marginBottom: 6 }}>\xf0\x9f\x96\xa5\xef\xb8\x8f {vm.name}</div>\r\n                                      <div style={{ display: "flex", alignItems: "center", gap: 0 }}>\r\n                                        {MTV_STAGES.map((stg, si) => {\r\n                                          const label = stg.replace(/([A-Z])/g, " $1").trim();\r\n                                          return <Fragment key={stg}>\r\n                                            <div style={{ display: "flex", flexDirection: "column", alignItems: "center", flex: 1 }}>\r\n                                              <div style={{ width: 28, height: 28, borderRadius: "50%", background: "#22c55e18", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 13, border: "2px solid #22c55e" }}><span style={{ color: "#22c55e", fontWeight: 700 }}>\\u2714</span></div>\r\n                                              <div style={{ fontSize: 10, marginTop: 3, color: "#22c55e", fontWeight: 600, textAlign: "center" }}>{label}</div>\r\n                                            </div>\r\n                                            {si < MTV_STAGES.length - 1 && <div style={{ flex: 0.6, height: 2, borderRadius: 1, background: "#22c55e", marginBottom: 16 }} />}\r\n                                          </Fragment>;\r\n                                        })}\r\n                                      </div>\r\n                                    </div>\r\n                                  );\r\n                                })}\r\n                              </div>\r\n                            )}\r\n                            {actions.length > 0 && ('''

raw = raw.replace(old4, mtv_pipeline)
print("4. MTV pipeline stages component added")

open(f, 'wb').write(raw)
print("All changes saved!")